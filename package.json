{
  "name": "lexscope-atlas",
  "version": "2.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "capture:social": "node scripts/capture-from-config.cjs --config capture-config.json",
    "capture:site": "node scripts/capture-from-config.cjs --config capture-config.json",
    "capture:install": "npx playwright install chromium"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
